#include "main.h"

int _printf(const char *format, ....)
{
	int chk_print = 0;

	va_list args_lst;

	if(format == NULL)
	{
		return (-1);
	}

	va_start(args_lst, format);

	while(*format)
	{
		if 
	}
	
	va_arg

	va_end
}